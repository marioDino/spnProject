{% extends 'base.html.twig' %}

{% block title %}LigneCommande index{% endblock %}

{% block body %}
    <h1>Ligne commande</h1>

    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quatité commandé</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ligne_commande in ligne_commandes %}
            <tr>
                <td>{{ ligne_commande.id }}</td>
                <td>{{ ligne_commande.qteComm }}</td>
                <td>
                  <!--  <a href="{{ path('ligne_commande_show', {'id': ligne_commande.id}) }}">show</a>-->
                    <a href="{{ path('ligne_commande_edit', {'id': ligne_commande.id}) }}">editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ligne_commande_new') }}">Nouveau</a>
{% endblock %}
